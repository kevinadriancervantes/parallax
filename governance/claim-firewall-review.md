# Adversarial claim/firewall review

## Review result

```text
SUPPORTED_NARROW_CLAIMS = retained
QUALIFIED_PATH_CLAIMS = explicitly labeled
PROHIBITED_BROAD_CLAIMS = caught and excluded
SCIENTIFIC_PROVIDER_RESULT = NONE
```

The baseline was searched for the following overclaims and corrected or
explicitly negated:

- belief updating, truth tracking, honesty, rationality, deception resistance;
- a frontier-provider scientific result;
- “Claude/Gemini passed” without the words “production-path qualification”;
- a confirmatory result;
- `N=9270` as 95% full-joint power;
- local capacity contrast as a causal parameter-count effect;
- stationarity as an assumed fact;
- surface-cue concordance as proof of mechanism.

The permitted language is narrower: the instrument and its failure modes were
exercised, the synthetic implementation was checked against known worlds, the
local surrogate exposed real pathologies, and current production transport
paths were qualified with non-scientific probes.

## Claim firewall invariants

The two rows in the claim matrix whose claims are `A scientific provider
result exists` and `Parallax identifies hidden belief revision` are explicit
prohibited-claim tests. They are not asserted Parallax findings. Their purpose
is to demonstrate that the release catches and excludes those readings.

1. Every provider claim says “path qualification” unless it is about a
   future, unexecuted confirmatory plan.
2. Every local-model claim says “descriptive surrogate” or “instrument
   exercise.”
3. Every synthetic claim says “known-ground-truth software/analysis evidence.”
4. Every confirmatory claim states `NOT_PERFORMED` and `FUNDING_DEPENDENT`.
5. Surface-cue findings are labeled diagnostic and non-mechanistic.
