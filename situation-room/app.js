(() => {
  "use strict";
  const COLORS={LEANS_NOT_H:"#ef8d80",UNRESOLVED:"#a4b1b5",LEANS_H:"#61d6c3",BEHAVIORAL_REFUSAL:"#b9a2ef",MEASUREMENT_UNAVAILABLE:"#efbd6e",TRANSPORT_UNAVAILABLE:"#75aee9",UNCLASSIFIED_UNAVAILABLE:"#d7e078"};
  const CASES={VELA:"PARALLAX_SR3_VELA_STRAIT",AFG:"AFGHANISTAN_1979"};
  let data=null,view="overview",model="QWEN_14B",caseId=CASES.VELA,surface="PRIMARY";
  const $=s=>document.querySelector(s);
  const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[c]));
  const pct=v=>`${(Number(v||0)*100).toFixed(1)}%`;
  const caseName=v=>v===CASES.VELA?"Vela Strait":v===CASES.AFG?"Afghanistan 1979":v;
  const modelName=v=>v==="QWEN_14B"?"Qwen 2.5 14B":v==="GEMMA_4B"?"Gemma 3 4B":v;
  const doseName=v=>data.dose_labels[v]||v;
  const labelState=v=>data.state_labels[v]||v;
  const rowsFor=(m,c,s)=>data.profiles.filter(p=>p.model_key===m&&p.case===c&&p.surface===s).sort((a,b)=>a.dose_id.localeCompare(b.dose_id));
  const stack=p=>{const n=p.n||p.common_denominator;return `<div class="bar" aria-label="Observable terminal-state distribution">${data.state_order.map(s=>`<span class="seg" style="width:${n?(p.counts[s]||0)/n*100:0}%;background:${COLORS[s]}" title="${esc(labelState(s))}: ${(p.counts[s]||0)}/${n}"></span>`).join("")}</div>`};
  const legend=()=>`<div class="legend">${data.state_order.map(s=>`<span><i class="swatch" style="background:${COLORS[s]}"></i>${esc(labelState(s))}</span>`).join("")}</div>`;
  const metrics=(label,value,sub,cl="")=>`<div class="metric ${cl}"><div class="label">${esc(label)}</div><div class="value">${esc(value)}</div><div class="sub">${esc(sub)}</div></div>`;
  const header=(k,t,d)=>`<div class="header"><div class="kicker">${esc(k)}</div><h2>${esc(t)}</h2><p>${esc(d)}</p></div>`;
  const pill=(t,c="teal")=>`<span class="pill ${c}">${esc(t)}</span>`;
  function doseRows(items){return `<div class="dose-list">${items.map(p=>`<div class="dose-row"><div class="dose-name"><strong>${esc(p.dose_id)}</strong>${esc(doseName(p.dose_id))}</div>${stack(p)}<div class="n">n=${p.n}</div></div>`).join("")}</div>${legend()}`;}
  function nav(){document.querySelectorAll(".nav-item").forEach(b=>b.classList.toggle("active",b.dataset.view===view));}
  function renderOverview(){const a=data.aggregate_observability;return `${header("Situation Room submission candidate","Instrumenting strategic judgment without an answer key","Parallax measures whether an AI system’s observable strategic assessment changes when prospectively controlled evidence changes—while keeping presentation variation, refusal, and execution failure visible.")}<section class="hero"><div class="hero-main"><div class="kicker">The contest question</div><div class="lede">When strategically relevant evidence changes, does the observable assessment move coherently—and how much of that behavior survives controlled nuisance variation and unavailable execution?</div><div class="body">Parallax is deliberately smaller than a wargame or simulator. It isolates evidence perturbation, controls nuisance presentation, records the response distribution, and refuses to convert missing observations into substantive judgments.</div><div class="pipeline"><div class="step"><strong>EVIDENCE</strong><span>Five frozen doses across a controlled case.</span></div><div class="step"><strong>MODEL</strong><span>Stochastic local language-model subjects.</span></div><div class="step"><strong>OBSERVATION</strong><span>Terminal states on one common denominator.</span></div></div></div><div class="hero-side">${metrics("Bounded claim","PASS WITH QUALIFICATIONS","SR-10 independent capstone","good")}${metrics("Governing rows","1,600","50 profiles × n=32")}${metrics("Transport unavailable","48.5625%","777 / 1,600 retained","alert")}${metrics("Frontier result","NONE","No frontier scientific campaign","amber")}</div></section><div class="grid two"><section class="card"><div class="card-head"><div><h3>What Parallax measures</h3><p>Observable evidence responsiveness under nuisance control.</p></div></div><div class="callout">It does not measure truth, correctness, hidden belief, reasoning mechanism, rationality, or general strategic competence.</div><div class="pills" style="margin-top:15px">${pill("Known-policy self-test")}${pill("Historical transfer")}${pill("Refusal separate","violet")}${pill("Unavailable retained","amber")}</div></section><section class="card"><div class="card-head"><div><h3>What the candidate does not claim</h3><p>These are load-bearing boundaries, not footnotes.</p></div></div><div class="callout warn">Local model subjects only. No frontier scientific result. No analyst or operational-utility evidence. Human domain SME validation remains outstanding.</div></section></div><section class="card" style="margin-top:16px"><div class="card-head"><div><h3>Evidence ladder</h3><p>One ladder, no composite score.</p></div></div><div class="key-value"><div class="key">Synthetic instrument recovery</div><div class="value">PASS</div><div class="key">Historical transfer</div><div class="value">PASS WITH QUALIFICATIONS · primary/N1 only</div><div class="key">Real local-model profiles</div><div class="value">ESTABLISHED WITH MAJOR OBSERVABILITY QUALIFICATION</div><div class="key">Proof of instrument</div><div class="value">PASS WITH QUALIFICATIONS</div><div class="key">Operational utility</div><div class="value">NOT EARNED</div></div></section>`;}
  function renderVela(){const ps=rowsFor(model,CASES.VELA,"PRIMARY");return `${header("Controlled fiction","Vela Strait","A fully fictional readiness case keeps the evidence geometry precise without pretending to simulate a strategic environment.")}<div class="case"><div class="kicker">Five-dose architecture</div><div class="hypothesis">The case asks the model to classify observable evidentiary support for a frozen readiness proposition.</div><div class="meta">${pill("5 evidence doses")}${pill("6 units per dose")}${pill("N1 nuisance family")}${pill("No ordinal mean","amber")}</div></div><section class="card" style="margin-top:16px"><div class="card-head"><div><h3>Observable distributions</h3><p>${esc(modelName(model))} · primary surface · common denominator n=32 per dose.</p></div></div>${doseRows(ps)}</section><section class="card" style="margin-top:16px"><div class="callout">The intended geometry is strong against → weak against → unresolved → weak toward → strong toward. The categories are ordered labels for observable support; they are not equally spaced numerical scores.</div></section>`;}
  function renderAfghanistan(){return `${header("Historical transfer","Afghanistan 1979","A historically sourced readiness case used as a transfer diagnostic—not as an outcome-accuracy benchmark.")}<div class="case"><div class="kicker">Frozen proposition</div><div class="hypothesis">${esc(data.cases[CASES.AFG].hypothesis)}</div><div class="meta">${pill("Cutoff: 20 Dec 1979 12:06Z")}${pill("Readiness / capability")}${pill("Outcome not an answer key","amber")}${pill("Primary + N1 only")}</div></div><div class="grid two" style="margin-top:16px"><section class="card"><div class="card-head"><div><h3>Why this case is admissible</h3><p>The five-dose ladder bears on force posture, airhead capability, movement, and mobilization requirements.</p></div></div><div class="callout">The historical outcome is excluded from the response key. Political intent may contextualize the record but cannot substitute for readiness evidence.</div></section><section class="card"><div class="card-head"><div><h3>Provenance</h3><p>Accepted source ledger.</p></div></div><div class="table-wrap"><table><thead><tr><th>Source</th><th>Date</th><th>Class</th></tr></thead><tbody>${data.cases[CASES.AFG].sources.map(s=>`<tr><td><a href="${esc(s.url)}" target="_blank" rel="noreferrer">${esc(s.source_id)}</a><br>${esc(s.title)}</td><td>${esc(s.document_date)}</td><td>${esc(s.classification)}</td></tr>`).join("")}</tbody></table></div></section></div><section class="card" style="margin-top:16px"><div class="callout warn">An attempted identity-masking diagnostic was invalidated during independent review and excluded. It is not shown as a performance comparison here.</div></section>`;}
  function controls(){return `<div class="controls"><div class="control"><label for="model">Model family</label><select id="model">${["QWEN_14B","GEMMA_4B"].map(v=>`<option value="${v}" ${v===model?"selected":""}>${esc(modelName(v))}</option>`).join("")}</select></div><div class="control"><label for="case">Case</label><select id="case"><option value="${CASES.VELA}" ${caseId===CASES.VELA?"selected":""}>Vela Strait</option><option value="${CASES.AFG}" ${caseId===CASES.AFG?"selected":""}>Afghanistan 1979</option></select></div><div class="control"><label for="surface">Presentation</label><select id="surface"><option value="PRIMARY" ${surface==="PRIMARY"?"selected":""}>Primary</option><option value="N1_B" ${surface==="N1_B"?"selected":""}>N1 neutral paraphrase</option></select></div></div>`;}
  function renderProfiles(){const ps=rowsFor(model,caseId,surface);return `${header("Real model evidence","Observable profile distributions","These are distributions of terminal classifications, not winners, scores, or recommendations.")}${controls()}<section class="card"><div class="card-head"><div><h3>${esc(modelName(model))} · ${esc(caseName(caseId))} · ${surface==="PRIMARY"?"primary":"N1 neutral paraphrase"}</h3><p>n=32 per dose. Unavailable mass remains visible in every row.</p></div></div>${doseRows(ps)}</section><section class="card" style="margin-top:16px"><div class="callout amber">The two model families demonstrate measurable traversal of the instrument. They do not establish frontier behavior, correctness, or that one model is better.</div></section>`;}
  function renderObservability(){const a=data.aggregate_observability,t=data.transport_root_cause;return `${header("Fail-closed observability","The missing mass stays in view","A failed observation is not silently converted into a substantive judgment or removed from the denominator.")}<div class="hero-side" style="margin-bottom:16px">${metrics("Transport unavailable","777 / 1,600","48.5625% retained","alert")}${metrics("Measurement unavailable",a.MEASUREMENT_UNAVAILABLE,"Observed response not classifiable","amber")}${metrics("Behavioral refusal",a.BEHAVIORAL_REFUSAL,"Separate observable state")}${metrics("Common denominator","1,600","All governing rows")}</div><div class="grid two"><section class="card"><div class="card-head"><div><h3>Terminal states</h3><p>Substantive categories, refusal, and technical states remain distinct.</p></div></div><div class="bars">${data.state_order.map(s=>`<div class="root-row"><div class="root-label">${esc(labelState(s))}</div><div class="root-bar"><div class="root-fill" style="width:${a.TOTAL?(a[s]||0)/a.TOTAL*100:0}%;background:${COLORS[s]}"></div></div><div class="root-n">${a[s]||0}</div></div>`).join("")}</div></section><section class="card"><div class="card-head"><div><h3>Transport root cause</h3><p>Frozen one-attempt local runtime episode; no retries or imputation.</p></div></div><div class="bars">${Object.entries(t.causes).map(([k,v])=>`<div class="root-row"><div class="root-label">${esc(k.replaceAll("_"," "))}</div><div class="root-bar"><div class="root-fill" style="width:${v/t.count*100}%"></div></div><div class="root-n">${v}</div></div>`).join("")}</div><div class="callout amber" style="margin-top:16px">The 777 rows are infrastructure unavailability, not model answers. No formal high-nonobservability threshold was defined.</div></section></div><section class="card" style="margin-top:16px"><div class="card-head"><div><h3>Partial identification</h3><p>What remains identifiable under missing mass.</p></div></div><div class="callout">When a response is unavailable, Parallax does not guess what the model would have said. It preserves the common denominator and reports coherent bounds with refusal separate and cumulative thresholds nested.</div></section>`;}
  function renderNuisance(){const n=data.nuisance_summary;return `${header("Controlled presentation","Nuisance sensitivity","The N1 surface changes presentation while preserving the decision-relevant task. Results are descriptive because no prospective equivalence margin was defined.")}<div class="hero-side" style="margin-bottom:16px">${metrics("Contrasts","20","Primary versus N1")}${metrics("Exact-zero","3","Descriptive only","good")}${metrics("Max state-share Δ","0.5625","Absolute difference")}${metrics("Max TV","0.5625","Distributional contrast")}</div><section class="card"><div class="callout amber">No affirmative behavioral-equivalence gate or threshold was prospectively defined. These values describe sensitivity; they do not certify nuisance stability.</div><div class="table-wrap" style="margin-top:16px"><table><thead><tr><th>Case</th><th>Model</th><th>Dose</th><th>State-share contrast</th><th>Cumulative contrast</th></tr></thead><tbody>${data.nuisance_contrasts.slice(0,20).map(x=>`<tr><td>${esc(caseName(x.case))}</td><td>${esc(modelName(x.model_key))}</td><td>${esc(x.dose_id)}</td><td class="mono">${esc(JSON.stringify(x.delta_right_minus_left))}</td><td class="mono">${esc(JSON.stringify(x.cumulative_delta_right_minus_left))}</td></tr>`).join("")}</tbody></table></div></section>`;}
  function renderKnown(){const k=data.known_policy;return `${header("Synthetic self-test","Known-policy recovery","The engine was challenged with prospectively known response structures before being interpreted on real model subjects.")}<section class="card"><div class="callout">${esc(k.interpretation)}</div><div class="table-wrap" style="margin-top:16px"><table><thead><tr><th>Policy</th><th>Known structure</th></tr></thead><tbody>${k.policies.map(p=>`<tr><td><strong>${esc(p.id)}</strong></td><td>${esc(p.meaning)}</td></tr>`).join("")}</tbody></table></div></section><section class="card" style="margin-top:16px"><div class="key-value"><div class="key">Gate</div><div class="value">PROOF_OF_INSTRUMENT_SYNTHETIC = PASS</div><div class="key">K12 seed</div><div class="value mono">20260824</div><div class="key">K12 n</div><div class="value">174</div><div class="key">Retries</div><div class="value">0</div></div></section>`;}
  function renderShortcuts(){const s=data.shortcut_diagnostics.summary;return `${header("Construct-validity attack","Shortcut diagnostics stay diagnostic","These fixtures ask whether shallow cue-compatible profiles appear. They do not identify internal mechanisms.")}<div class="hero-side" style="margin-bottom:16px">${metrics("Adjudications","16","Two local model families × eight fixtures")}${metrics("Inconclusive","10","Cannot distinguish","amber")}${metrics("No compatibility observed","6","No tested compatible profile","good")}${metrics("Compatible profile","0","No mechanism inference")}</div><section class="card"><div class="callout amber">Correct interpretation: no tested shortcut-compatible profile was observed in six adjudications; ten were inconclusive. This is not a proof that shortcuts were absent.</div><div class="table-wrap" style="margin-top:16px"><table><thead><tr><th>Model</th><th>Fixture</th><th>Status</th><th>Target cue</th></tr></thead><tbody>${data.shortcut_diagnostics.rows.map(r=>`<tr><td>${esc(modelName(r.model_key))}</td><td>${esc(r.fixture_id)}</td><td class="state ${r.status.includes("INCONCLUSIVE")?"unresolved":"leans-h"}">${esc(r.status)}</td><td>${esc(r.target_surface_cue)}</td></tr>`).join("")}</tbody></table></div></section>`;}
  function renderProvenance(){return `${header("Inspectability","Provenance and reproduction","The contest candidate is a deterministic derivative of sealed predecessor evidence. Large manifests remain available in the private record, while the judge path stays readable.")}<section class="grid two"><div class="card"><div class="card-head"><div><h3>Binding identities</h3><p>Accepted evidence carried into this candidate.</p></div></div><div class="key-value">${Object.entries(data.provenance).map(([k,v])=>`<div class="key">${esc(k)}</div><div class="value mono">${esc(v)}</div>`).join("")}</div></div><div class="card"><div class="card-head"><div><h3>What can be reconstructed</h3><p>No provider call is required for the public-safe inspection path.</p></div></div><div class="callout">A reviewer can inspect the five-dose geometry, known-policy self-test, primary/N1 profiles, observability denominator, partial-identification rule, shortcut dispositions, source links, and exact claim boundary. Model weights and private raw envelopes are not included in this projection.</div><div class="pills" style="margin-top:15px">${pill("Deterministic data projection")}${pill("SR-10 bound")}${pill("No live model required","amber")}</div></div></section>`;}
  function renderLimitations(){return `${header("Claim boundary","What Parallax does not answer","The narrowness is deliberate. The candidate makes the earned instrument claim legible without turning it into a certification badge.")}<section class="card"><div class="pills">${data.mandatory_qualifications.map((q,i)=>pill(q,i===2||i===5?"amber":"coral")).join("")}</div></section><div class="grid two" style="margin-top:16px"><section class="card"><div class="card-head"><div><h3>Invalid branch</h3><p>Transparent governance disclosure.</p></div></div><div class="callout warn">The attempted identity-masking diagnostic was invalidated because provider-visible H and source identifiers remained unmasked. Its 320 observations and 10 contrasts are excluded from this governing candidate.</div></section><section class="card"><div class="card-head"><div><h3>Future tracking</h3><p>What the frozen instrument could measure later.</p></div></div><div class="callout">Future model/version campaigns could track evidence-response geometry, nuisance sensitivity, refusal, and observability under an unchanged contract. Current longitudinal, analyst-utility, and frontier results do not exist.</div></section></div>`;}
  function enhanceOverview(){if(view!=="overview"||$("#r1-presentation"))return;$("#view").insertAdjacentHTML("beforeend",`<section id="r1-presentation" class="card" style="margin-top:16px"><div class="card-head"><div><h3>Which local evidence governs this console?</h3><p>One clear lineage for the contest-facing claim.</p></div></div><div class="callout">Earlier local evidence used a 4,800-row Qwen campaign for surrogate characterization. This Situation Room uses the later SR-8R1 full-bundle campaign: 1,600 rows across two independent local model families. The earlier campaign remains preserved historical/non-governing diagnostic evidence; SR-8 V1 and earlier local evidence are not pooled into these governing profiles. For the contest-facing scientific claim, the 1,600-row SR-8R1 result is the governing layer and supersedes the earlier row-count presentation.</div></section><section class="card" style="margin-top:16px"><div class="card-head"><div><h3>What a decision-maker can learn</h3><p>Useful today, terrible at, and track over time—without implying correctness or utility.</p></div></div><div class="grid three"><div class="callout"><strong>Useful today</strong><br>Screen whether an observable assessment moves with evidence, reveals presentation fragility, and remains observable. If a profile is unstable or unavailable, seek independent verification rather than relying on it. Improved decision quality and analyst utility have not been demonstrated.</div><div class="callout warn"><strong>Terrible at</strong><br>Expose measurable failure modes such as evidence-insensitive behavior, nuisance-sensitive behavior, selective refusal, unavailable execution, and shortcut-compatible or inconclusive diagnostics. These are not claims that a model is bad at strategy.</div><div class="callout amber"><strong>Track over time</strong><br>Rerun the same frozen evidence geometry on later model or configuration snapshots. Compare full response distributions, nuisance sensitivity, refusal, unavailability, and shortcut diagnostics while preserving each campaign’s model, provider, time, and configuration boundary. No longitudinal result exists yet.</div></div></section>`);}
  function render(){nav();const f={overview:renderOverview,vela:renderVela,afghanistan:renderAfghanistan,profiles:renderProfiles,observability:renderObservability,nuisance:renderNuisance,"known-policy":renderKnown,shortcuts:renderShortcuts,provenance:renderProvenance,limitations:renderLimitations}[view]||renderOverview;$("#view").innerHTML=f();bind();enhanceOverview();}
  function bind(){document.querySelectorAll(".nav-item").forEach(b=>b.addEventListener("click",()=>{view=b.dataset.view;render()}));const m=$("#model"),c=$("#case"),s=$("#surface");if(m)m.addEventListener("change",e=>{model=e.target.value;render()});if(c)c.addEventListener("change",e=>{caseId=e.target.value;surface="PRIMARY";render()});if(s)s.addEventListener("change",e=>{surface=e.target.value;render()});}
  fetch("data/data.json",{cache:"no-store"}).then(r=>r.json()).then(d=>{data=d;$("#build-id").textContent=`projection ${d.projection_identity.slice(0,10)}…`;render()}).catch(e=>{$("#view").innerHTML=`<div class="empty">Projection failed to load: ${esc(e.message)}</div>`});
})();
